//import { Rr } from "./index";
//import { shallow, mount, render } from "enzyme";
it("works", () => {
  expect(2).toEqual(2);
});

it("doesn't work", () => {
  expect(2).toEqual(2);
});

//const wrapper = shallow(<Rr />);
